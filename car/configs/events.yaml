arrow:
  data_dir: car/data/events/arrows
  classes: [left, right]
  input_size: 96
  batch_size: 32
  val_ratio: 0.2
  epochs: 30
  lr: 0.001
  weight_decay: 0.0001
  warmup_epochs: 3
  early_stopping_patience: 8
  dropout: 0.2
  pretrained: true
  hflip: false
