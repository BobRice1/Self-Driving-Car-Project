experiment:
  run_name: effnetv2s_384x288

model:
  backbone: tf_efficientnetv2_s
  pretrained: true
  hidden_dim: 256
  dropout: 0.2

data:
  width: 384
  height: 288
  batch_size: 40
  val_batch_size: 80
  num_workers: 4
  hflip_p: 0.2

training:
  epochs: 20
  lr: 0.00025
  weight_decay: 0.0001
  warmup_epochs: 1
  mixed_precision: true
  early_stopping_patience: 4
  grad_clip_norm: 1.0
  angle_loss_weight: 2.5
  speed_loss_weight: 1.0
  angle_sample_weight_alpha: 3.0
